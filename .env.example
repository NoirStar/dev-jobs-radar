# ============================================================
# DevJobsRadar 환경변수 설정
# .env.local 파일로 복사하여 사용하세요
# ============================================================

# ── Supabase (필수) ──
# Supabase Dashboard → Settings → API 에서 확인
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ── Supabase Service Role (서버 전용, Vercel 환경변수에 설정) ──
# Cron 수집기가 DB에 쓸 때 사용 (클라이언트에 노출 금지!)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Vercel Cron 인증 ──
CRON_SECRET=your-cron-secret

# ── OpenAI (선택 — AI 기능 사용 시) ──
VITE_OPENAI_PROXY_URL=https://your-proxy.example.com/api/openai
